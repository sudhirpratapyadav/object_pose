# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env

# uv
.uv-cache/

# Model weights (large, downloaded on first run)
weights/

# Dataset files (big media; not part of the repo)
datasets/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Active calibration configs — these hold per-machine values. Commit only
# the .example templates so users see the shape but bring their own.
cam_calib_config.yaml
sim_config.yaml
cam_factory_intrinsics.yaml
